import styled from '@emotion/styled';
import {
  background,
  color,
  flexbox,
  layout,
  shadow,
  space,
  typography,
} from 'styled-system';

export const Box = styled('div')(
  color,
  space,
  layout,
  flexbox,
  typography,
  background,
  shadow,
);
